#include<iostream>
using namespace std;
int main(){
	int arr[] = {7,1,5,3,6,4};
	int maxprofit = 0,bestbuy = arr[0],n = sizeof(arr)/sizeof(int);
	for(int i=1;i<n;i++){
		if(arr[i]>bestbuy){
			maxprofit = max(maxprofit,arr[i]-bestbuy);
		}
		bestbuy = min(bestbuy,arr[i]);
	}
	cout<<maxprofit;
}
